@model IRS.Web.ViewModels.Account.RegisterViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Register";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Register</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" />
    <link href="~/Content/login.css" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <div class="text-center login-title">@Html.PageTitle("Register")</div>
                <br />
                <div class="account-wall">
                    <img class="profile-img" src="~/Content/Images/login-photo.png"
                         alt="">
                    @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-signin" }))
                    {
                        @Html.AntiForgeryToken()
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = Labels.Email, autofocus = "autofocus" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control", @placeholder = "First name", autocomplete = "off" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.LastName, new { @class = "form-control", @placeholder = "Last name",  autocomplete = "off" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.NewPassword, new { @class = "form-control", @placeholder = Labels.NewPassword, type = "password", autocomplete = "off" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "form-control", @placeholder = Labels.ConfirmPassword, type = "password", autocomplete = "off" })
                        </div>
                        <button class="btn btn-lg btn-primary btn-block" type="submit">
                            Register
                        </button>
                        <span class="clearfix"></span>
                    }
                </div>
            </div>
        </div>
    </div>
</body>
</html>
